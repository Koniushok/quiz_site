import React from "react";
import { LogoText } from "./styles/styled.js";

export const Account = props => {
  return <React.Fragment>Account-</React.Fragment>;
};

export const Logo = props => {
  return <LogoText>Val.Bat</LogoText>;
};
